# ESP32 語音播報 重點與步驟整理

---

## 一、重點整理

### 1. 兩個 IDE 分開使用

| IDE 版本                     | 用途                                |
| -------------------------- | --------------------------------- |
| **Arduino IDE 1.8.19（舊版）** | 只用來上傳音檔（ESP32 Sketch Data Upload） |
| **Arduino IDE 2.x（新版）**    | 寫程式、燒錄邏輯、看序列埠、除錯                |

### 2. 音檔規格

* 格式：**WAV**
* 取樣率：**16000 Hz**
* 聲道：**單聲道（Mono）**
* 位元深度：**16-bit PCM**
* ❌ 不要用 **MP3**（解碼太耗資源，可能沒聲音）
* ✅ 檔名建議用英文或數字，例如 `0001.wav`，不要有中文或空格

### 3. 沒聲音常見原因

* 音檔不是 16000Hz WAV 單聲道
* 檔名或路徑錯誤（程式裡要用 `/檔名.wav`）
* 音檔沒有實際上傳到 ESP32
* 上傳 data 時沒有關閉序列埠監視器
* 程式使用的檔案系統（SPIFFS / LittleFS）不一致

---

## 二、完整操作步驟

### 階段 A：準備音檔與 data 資料夾

1. **轉換音檔**：用 Audacity 或線上工具，設定 **16000Hz、單聲道**，匯出 **WAV（16-bit PCM）**
2. **命名音檔**：用簡單英文或數字，例如 `0001.wav`
3. **建立 data 資料夾**：在 `.ino` 檔案旁邊建立 `data` 資料夾，將 `.wav` 檔放入

資料夾結構：

```text
你的專案/
├── 你的專案.ino
└── data/
    └── 0001.wav
```

---

### 階段 B：用舊版 IDE 1.8.19 上傳音檔

1. **開啟 Arduino IDE 1.8.19**
2. 打開你的專案 `.ino` 檔
3. **關閉序列埠監視器**（重要，否則上傳會失敗）
4. 確認工具設定：

   * 開發板：正確的 ESP32 型號
   * 連接埠：正確的 COM Port
   * Flash Size / Partition Scheme：選擇有 **SPIFFS** 或 **LittleFS** 的分區
5. 點選 **「工具」→「ESP32 Sketch Data Upload」**
6. 等待進度條跑到 **100%**
7. 關閉舊版 IDE

> 如果工具選單沒有「ESP32 Sketch Data Upload」，表示尚未安裝 ESP32FS 外掛。

---

### 階段 C：用新版 IDE 2.x 修改程式並燒錄

1. 開啟 **Arduino IDE 2.x**
2. 打開同一個專案
3. 確認程式中有初始化檔案系統，例如：

   * `SPIFFS.begin(true);` 或 `LittleFS.begin(true);`
   * 以及音訊播放模組的初始化
4. 在對應的 `if` 判斷式內加入：

```cpp
playWav("/0001.wav");
```

* 路徑前面一定要有 `/`
* 檔名要和 `data` 資料夾內的檔名完全一致

5. 選擇正確的開發板與連接埠
6. 點擊上傳燒錄程式
7. 燒錄完成後，打開序列埠監視器測試

---

## 三、如果還是沒聲音，依序檢查

* [ ] 音檔是否為 **16000Hz、單聲道、WAV、16-bit PCM**
* [ ] 音檔是否放在 `data` 資料夾中
* [ ] 是否有用舊版 IDE 執行 **ESP32 Sketch Data Upload**
* [ ] 上傳 data 時是否已關閉序列埠監視器
* [ ] 程式中的路徑是否為 `/檔名.wav`
* [ ] 程式使用的檔案系統（SPIFFS / LittleFS）是否和上傳方式一致
* [ ] 音訊輸出硬體是否正確（DAC、I2S、功放、喇叭）
* [ ] 音量設定是否過小或未開啟
